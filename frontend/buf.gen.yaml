version: v2
plugins:
  # Connect-ES v2: protoc-gen-es emits both messages and service descriptors.
  # Clients are created at runtime with createClient(Service, transport).
  - local: protoc-gen-es
    out: src/gen
    opt:
      - target=ts
